{#
  Link-Block — deterministic param API

  Expects (via linkBlockParams):
  - hasSecondary: boolean
  - primary: { priority, label, URL, link }
  - secondary: { priority, label, URL, link }
#}

{% set p = linkBlockParams %}

<div class="link-block">

  {% if p.primary %}
    {% set linkParams = p.primary %}
    {% include "components/link.njk" %}
  {% endif %}

  {% if p.hasSecondary and p.secondary %}
    {% set linkParams = p.secondary %}
    {% include "components/link.njk" %}
  {% endif %}

</div>
