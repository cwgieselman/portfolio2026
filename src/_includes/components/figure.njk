{# Figure Component — passthrough image v1 #}

<figure class="figure figure--{{ type }}">
  <div class="figure__media">
    {% if hasAlt %}
      <img src="{{ src }}" alt="{{ alt }}" loading="lazy" decoding="async" />
    {% else %}
      <img src="{{ src }}" alt="" role="presentation" loading="lazy" decoding="async" />
    {% endif %}
  </div>

  {% if showCaption and caption %}
    <figcaption class="figure__caption">{{ caption }}</figcaption>
  {% endif %}
</figure>
